class Solution {
public:
    int mySqrt(int x) {
        int l = 0;
        int r = n;
        int mid;

        while (l < r) {
            mid = (l + r) / 2;
            if (mid*mid )
        }
    }
};